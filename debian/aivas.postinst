#!/bin/bash
set -e

case "$1" in
    configure)
        echo "Installing AIVAS Python package and dependencies..."
        python3 -m pip install /usr/share/aivas/aivas-*.whl \
            --break-system-packages --quiet --no-warn-script-location || true
        ;;
esac

#DEBHELPER#
exit 0
