# AIVAS Web UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `aivas serve` command that starts a FastAPI server hosting a full-page AI-led chat web UI — users type naturally, scans run via WebSocket, results appear as inline cards.

**Architecture:** FastAPI serves a single `frontend/index.html` (vanilla JS, no build step) at `/` and exposes REST + WebSocket endpoints at `/api/`. The scan pipeline is reimplemented in `aivas/server/scan_worker.py` as an async generator that yields JSON progress events — no TUI dependency. The existing `run_agent()` from `aivas/tui/agent.py` is reused with a lightweight `SimpleNamespace(conn=conn)` shim.

**Tech Stack:** FastAPI ≥ 0.110, uvicorn[standard] ≥ 0.27, existing aivas Python package, vanilla JS (no framework, no npm), SQLite.

## Global Constraints

- No Python source file exceeds 200 lines. Split if approaching the limit.
- `aivas/server/` must not import from `aivas/tui/` except `aivas.tui.agent.run_agent` (which has no TUI runtime dependency — its `AIVASApp` reference is TYPE_CHECKING only).
- Python ≥ 3.10.
- Git identity for all commits: `Baraka Malila <bmalila87@gmail.com>`.
- All existing tests must continue to pass: `pytest tests/ -v`.
- Frontend is a single `frontend/index.html` — HTML + CSS + JS in one file, served directly. No build step, no npm.
- Run tests: `cd /home/cyberpunk/aivas && pytest tests/ -v`.

---

## File Structure

```
aivas/
  server/
    __init__.py          NEW — empty package marker
    main.py              NEW — FastAPI app, all routes, pending scan registry
    scan_worker.py       NEW — async-generator scan pipeline (no TUI deps)
    chat_api.py          NEW — wraps run_agent for HTTP context
  frontend/
    index.html           NEW — complete web UI (HTML + CSS + JS)
  cli.py                 MODIFY — add `serve` subcommand (~20 lines added)
  pyproject.toml         MODIFY — add fastapi, uvicorn[standard] dependencies
tests/
  server/
    __init__.py          NEW — empty
    conftest.py          NEW — test_conn and client fixtures
    test_endpoints.py    NEW — REST endpoint tests
    test_scan_worker.py  NEW — scan worker unit tests
```

---

### Task 1: Package scaffold, `aivas serve` CLI, health endpoint, frontend placeholder

**Files:**
- Create: `aivas/server/__init__.py`
- Create: `aivas/server/main.py`
- Create: `frontend/index.html`
- Modify: `aivas/cli.py`
- Modify: `pyproject.toml`
- Create: `tests/server/__init__.py`
- Create: `tests/server/conftest.py`
- Create: `tests/server/test_endpoints.py` (health check only for now)

**Interfaces:**
- Produces: `aivas.server.main.app` (FastAPI instance), `aivas.server.main._conn` (module-level `sqlite3.Connection | None`), `GET /health → {"status": "ok"}`, `GET / → index.html`

- [ ] **Step 1: Add fastapi and uvicorn to pyproject.toml**

Open `pyproject.toml` and add to the `dependencies` list (after `pyyaml>=6.0`):

```toml
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
```

- [ ] **Step 2: Install new deps**

```bash
cd /home/cyberpunk/aivas && pip install -e ".[dev]" -q
```

Expected: installs fastapi and uvicorn without errors.

- [ ] **Step 3: Write failing test for health endpoint**

Create `tests/server/__init__.py` (empty file).

Create `tests/server/conftest.py`:

```python
import sqlite3
import pytest
import aivas.server.main as _server
from aivas.database.schema import create_schema
from fastapi.testclient import TestClient


@pytest.fixture
def test_conn(tmp_path):
    conn = sqlite3.connect(str(tmp_path / "test.db"))
    conn.row_factory = sqlite3.Row
    create_schema(conn)
    yield conn
    conn.close()


@pytest.fixture
def client(test_conn, monkeypatch):
    # Set _conn before lifespan runs so lifespan skips DB init
    monkeypatch.setattr(_server, "_conn", test_conn)
    with TestClient(_server.app, raise_server_exceptions=True) as c:
        # Re-apply after lifespan in case it overwrote
        monkeypatch.setattr(_server, "_conn", test_conn)
        yield c
```

Create `tests/server/test_endpoints.py`:

```python
def test_health(client):
    r = client.get("/health")
    assert r.status_code == 200
    assert r.json() == {"status": "ok"}
```

- [ ] **Step 4: Run test, verify it fails**

```bash
cd /home/cyberpunk/aivas && pytest tests/server/test_endpoints.py::test_health -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'aivas.server'`

- [ ] **Step 5: Create the server package**

Create `aivas/server/__init__.py` (empty).

Create `aivas/server/main.py`:

```python
"""FastAPI web server for AIVAS — routes, WebSocket scan handler, pending registry."""
from __future__ import annotations

import sqlite3
import uuid
from contextlib import asynccontextmanager
from pathlib import Path

from fastapi import FastAPI, HTTPException, WebSocket, WebSocketDisconnect
from fastapi.responses import FileResponse
from pydantic import BaseModel

from aivas.database.schema import get_db, create_schema, DB_PATH
from aivas.history import list_scans, get_scan_findings

# In-memory map of scan_key → (target, level) for pending WebSocket scans
_pending: dict[str, tuple[str, int]] = {}
_conn: sqlite3.Connection | None = None

_FRONTEND = Path(__file__).parent.parent.parent / "frontend" / "index.html"


@asynccontextmanager
async def lifespan(app: FastAPI):
    global _conn
    if _conn is None:          # allow test injection via monkeypatch
        _conn = get_db(DB_PATH)
        create_schema(_conn)
    yield


app = FastAPI(lifespan=lifespan)


@app.get("/")
async def index():
    return FileResponse(_FRONTEND)


@app.get("/health")
async def health():
    return {"status": "ok"}


@app.get("/api/history")
async def history(limit: int = 10):
    return list_scans(_conn, limit=limit)


@app.get("/api/scan/{scan_id}")
async def get_scan(scan_id: int):
    findings = get_scan_findings(_conn, scan_id)
    if not findings and not _conn.execute(
        "SELECT 1 FROM scans WHERE id = ?", (scan_id,)
    ).fetchone():
        raise HTTPException(status_code=404, detail="Scan not found")
    return findings


class ChatRequest(BaseModel):
    text: str


@app.post("/api/chat")
async def chat(body: ChatRequest):
    from aivas.server.chat_api import handle_chat
    response, scan_intent = await handle_chat(_conn, body.text)
    scan_key = None
    if scan_intent:
        scan_key = str(uuid.uuid4())
        _pending[scan_key] = scan_intent      # (target, level)
    return {"response": response, "scan_id": scan_key}


@app.websocket("/ws/scan/{scan_key}")
async def scan_ws(websocket: WebSocket, scan_key: str):
    await websocket.accept()
    entry = _pending.pop(scan_key, None)
    if not entry:
        await websocket.send_json({"type": "error", "text": "Unknown scan key."})
        await websocket.close()
        return
    target, level = entry
    from aivas.server.scan_worker import run_scan
    scan_gen = run_scan(_conn, target, level)
    try:
        async for event in scan_gen:
            await websocket.send_json(event)
    except WebSocketDisconnect:
        pass
    finally:
        await scan_gen.aclose()
```

- [ ] **Step 6: Create placeholder frontend**

Create `frontend/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>AIVAS</title>
<style>
  body { background: #0a0a0a; color: #e0e0e0; font-family: system-ui, sans-serif;
         display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
  h1 { color: #4a9eff; }
</style>
</head>
<body>
  <h1>✦ AIVAS</h1>
</body>
</html>
```

- [ ] **Step 7: Add `serve` subcommand to cli.py**

Open `aivas/cli.py`. After the `interactive` import block (around line 15), the file already has `cli.add_command(doctor)` near the end. Add this command before `def main()`:

```python
@cli.command()
@click.option("--host", default="127.0.0.1", show_default=True)
@click.option("--port", default=8000, show_default=True)
@click.option("--open", "open_browser", is_flag=True, help="Open browser after start.")
def serve(host: str, port: int, open_browser: bool) -> None:
    """Start the AIVAS web UI server."""
    import uvicorn
    console.print(f"[bold cyan]✦ AIVAS web UI[/bold cyan]  →  http://{host}:{port}")
    if open_browser:
        import threading
        import webbrowser
        threading.Timer(1.2, lambda: webbrowser.open(f"http://{host}:{port}")).start()
    uvicorn.run("aivas.server.main:app", host=host, port=port, reload=False)
```

Also add `cli.add_command(serve)` after the existing `cli.add_command(doctor)` line.

- [ ] **Step 8: Run test, verify it passes**

```bash
cd /home/cyberpunk/aivas && pytest tests/server/test_endpoints.py::test_health -v
```

Expected: PASS

- [ ] **Step 9: Smoke-test the CLI command exists**

```bash
cd /home/cyberpunk/aivas && python -m aivas serve --help
```

Expected: Shows `--host`, `--port`, `--open` options without error.

- [ ] **Step 10: Commit**

```bash
cd /home/cyberpunk/aivas && git add aivas/server/__init__.py aivas/server/main.py frontend/index.html aivas/cli.py pyproject.toml tests/server/__init__.py tests/server/conftest.py tests/server/test_endpoints.py
git commit -m "feat(web): FastAPI server scaffold, aivas serve command, health endpoint"
```

---

### Task 2: History and scan detail REST endpoints

**Files:**
- Modify: `tests/server/test_endpoints.py` (add history and scan tests)

`main.py` already has the routes from Task 1 — this task adds tests and verifies them.

**Interfaces:**
- Consumes: `GET /api/history?limit=N`, `GET /api/scan/{id}` (already in main.py)
- Produces: verified endpoint contracts used by the frontend in Task 6

- [ ] **Step 1: Write failing tests**

Add to `tests/server/test_endpoints.py`:

```python
from aivas.history import save_scan
import aivas.server.main as _m


def test_history_empty(client):
    r = client.get("/api/history")
    assert r.status_code == 200
    assert r.json() == []


def test_history_returns_scans(client, test_conn, monkeypatch):
    monkeypatch.setattr(_m, "_conn", test_conn)
    save_scan(test_conn, "192.168.1.1", [])
    r = client.get("/api/history?limit=5")
    assert r.status_code == 200
    data = r.json()
    assert len(data) == 1
    assert data[0]["target"] == "192.168.1.1"
    assert "grade" in data[0]
    assert "risk_score" in data[0]


def test_get_scan_not_found(client):
    r = client.get("/api/scan/9999")
    assert r.status_code == 404


def test_get_scan_findings(client, test_conn, monkeypatch):
    monkeypatch.setattr(_m, "_conn", test_conn)
    findings = [{"cve_id": "CVE-2021-44228", "cvss_score": 10.0,
                 "cvss_severity": "CRITICAL", "confidence": "probable",
                 "host": "192.168.1.1"}]
    scan_id = save_scan(test_conn, "192.168.1.1", findings)
    r = client.get(f"/api/scan/{scan_id}")
    assert r.status_code == 200
    data = r.json()
    assert len(data) == 1
    assert data[0]["cve_id"] == "CVE-2021-44228"
```

- [ ] **Step 2: Run tests, verify they pass**

```bash
cd /home/cyberpunk/aivas && pytest tests/server/test_endpoints.py -v
```

Expected: all 5 tests PASS (health + 4 new ones). The routes were already implemented in Task 1.

- [ ] **Step 3: Commit**

```bash
cd /home/cyberpunk/aivas && git add tests/server/test_endpoints.py
git commit -m "test(web): add REST endpoint tests for history and scan detail"
```

---

### Task 3: Scan worker — async generator pipeline without TUI dependency

**Files:**
- Create: `aivas/server/scan_worker.py`
- Create: `tests/server/test_scan_worker.py`

**Interfaces:**
- Produces: `run_scan(conn: sqlite3.Connection, target: str, level: int = 2) -> AsyncGenerator[dict, None]`
  - Yields `{"type": "progress", "text": str}`
  - Yields `{"type": "error", "text": str}` (terminal — no done event follows)
  - Yields `{"type": "done", "target": str, "scan_id": int, "score": int, "grade": str, "service_count": int, "findings": list[dict]}`

- [ ] **Step 1: Write failing tests**

Create `tests/server/test_scan_worker.py`:

```python
import asyncio
import sqlite3
from unittest.mock import patch
import pytest
from aivas.database.schema import create_schema
from aivas.server.scan_worker import run_scan


@pytest.fixture
def conn(tmp_path):
    db = sqlite3.connect(str(tmp_path / "test.db"))
    db.row_factory = sqlite3.Row
    create_schema(db)
    yield db
    db.close()


async def _collect(gen):
    events = []
    async for ev in gen:
        events.append(ev)
    return events


def test_run_scan_nmap_error(conn):
    """nmap failure yields error event."""
    with patch("aivas.server.scan_worker._run_nmap",
               side_effect=RuntimeError("nmap not found")):
        events = asyncio.run(_collect(run_scan(conn, "192.168.1.1")))
    assert events[-1]["type"] == "error"
    assert "nmap not found" in events[-1]["text"]


def test_run_scan_no_open_ports(conn):
    """nmap returns XML with no services → error event."""
    with patch("aivas.server.scan_worker._run_nmap", return_value="<nmaprun/>"):
        with patch("aivas.server.scan_worker.parse_nmap_xml", return_value=[]):
            events = asyncio.run(_collect(run_scan(conn, "192.168.1.99")))
    assert events[-1]["type"] == "error"
    assert "no open ports" in events[-1]["text"]


def test_run_scan_done_event_shape(conn):
    """With services but no CVEs: done event has correct shape and grade A."""
    fake_service = {"host": "192.168.1.1", "port": 80, "service": "http",
                    "product": "apache", "version": "2.4", "os_family": None}
    with patch("aivas.server.scan_worker._run_nmap", return_value="<xml/>"):
        with patch("aivas.server.scan_worker.parse_nmap_xml", return_value=[fake_service]):
            with patch("aivas.server.scan_worker.correlate", return_value=[]):
                events = asyncio.run(_collect(run_scan(conn, "192.168.1.1")))
    done = events[-1]
    assert done["type"] == "done"
    assert done["target"] == "192.168.1.1"
    assert done["grade"] == "A"        # no findings → score 100
    assert done["score"] == 100
    assert done["service_count"] == 1
    assert isinstance(done["findings"], list)
    assert isinstance(done["scan_id"], int)


def test_run_scan_emits_progress_events(conn):
    """Multiple progress events emitted before done."""
    fake_service = {"host": "192.168.1.1", "port": 22, "service": "ssh",
                    "product": "openssh", "version": "7.4", "os_family": None}
    with patch("aivas.server.scan_worker._run_nmap", return_value="<xml/>"):
        with patch("aivas.server.scan_worker.parse_nmap_xml", return_value=[fake_service]):
            with patch("aivas.server.scan_worker.correlate", return_value=[]):
                events = asyncio.run(_collect(run_scan(conn, "192.168.1.1")))
    progress = [e for e in events if e["type"] == "progress"]
    assert len(progress) >= 2
```

- [ ] **Step 2: Run tests, verify they fail**

```bash
cd /home/cyberpunk/aivas && pytest tests/server/test_scan_worker.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'aivas.server.scan_worker'`

- [ ] **Step 3: Implement scan_worker.py**

Create `aivas/server/scan_worker.py`:

```python
"""Scan pipeline for the web server: runs nmap + CVE correlation, yields JSON events."""
from __future__ import annotations

import asyncio
import shutil
import sqlite3
import subprocess
from typing import AsyncGenerator

from aivas.correlator import correlate
from aivas.history import save_scan
from aivas.parser import parse_nmap_xml
from aivas.scanner.nse import scripts_for_level
from aivas.scorer import score_findings


async def _run_nmap(target: str, scripts: str, timeout: int = 300) -> str:
    """Run nmap -sV, return XML stdout. Raises RuntimeError on failure."""
    nmap_bin = shutil.which("nmap") or "nmap"
    cmd = [nmap_bin, "-sV", "-oX", "-", target]
    if scripts:
        cmd += ["--script", scripts]

    def _blocking() -> str:
        result = subprocess.run(cmd, capture_output=True, timeout=timeout)
        if result.returncode != 0:
            raise RuntimeError(
                f"nmap exited {result.returncode}: {result.stderr.decode()[:200]}"
            )
        return result.stdout.decode()

    return await asyncio.to_thread(_blocking)


async def run_scan(
    conn: sqlite3.Connection, target: str, level: int = 2
) -> AsyncGenerator[dict, None]:
    """Full scan pipeline as an async generator.

    Yields progress events then a single done (or error) terminal event.
    Event shapes:
      {"type": "progress", "text": str}
      {"type": "error",    "text": str}       — terminal, no done follows
      {"type": "done",     "target": str, "scan_id": int, "score": int,
       "grade": str, "service_count": int, "findings": list[dict]}
    """
    yield {"type": "progress", "text": f"Running port discovery on {target}…"}
    try:
        xml = await _run_nmap(target, scripts_for_level(level))
    except RuntimeError as exc:
        yield {"type": "error", "text": str(exc)}
        return

    try:
        services = parse_nmap_xml(xml)
    except Exception as exc:
        yield {"type": "error", "text": f"Could not parse nmap output: {exc}"}
        return

    if not services:
        yield {"type": "error",
               "text": f"{target}: no open ports found — host may be offline or firewalled."}
        return

    yield {"type": "progress",
           "text": f"Detected {len(services)} service(s). Correlating CVEs…"}

    os_hint = services[0].get("os_family") or None
    findings = [
        f for f in correlate(conn, services, os_hint=os_hint)
        if f.get("confidence") in ("probable", "confirmed")
    ][:30]

    yield {"type": "progress",
           "text": f"Found {len(findings)} CVE(s). Saving results…"}

    scored = score_findings(findings)
    scan_id = save_scan(conn, target, findings)

    yield {
        "type": "done",
        "target": target,
        "scan_id": scan_id,
        "score": scored["score"],
        "grade": scored["grade"],
        "service_count": len(services),
        "findings": [
            {
                "cve_id": f["cve_id"],
                "cvss_score": f.get("cvss_score"),
                "cvss_severity": f.get("cvss_severity"),
                "description": f.get("description") or "",
                "confidence": f.get("confidence"),
            }
            for f in findings
        ],
    }
```

- [ ] **Step 4: Run tests, verify they pass**

```bash
cd /home/cyberpunk/aivas && pytest tests/server/test_scan_worker.py -v
```

Expected: all 4 tests PASS.

- [ ] **Step 5: Run full test suite to check no regressions**

```bash
cd /home/cyberpunk/aivas && pytest tests/ -v --tb=short 2>&1 | tail -20
```

Expected: all tests pass.

- [ ] **Step 6: Commit**

```bash
cd /home/cyberpunk/aivas && git add aivas/server/scan_worker.py tests/server/test_scan_worker.py
git commit -m "feat(web): scan worker as async generator — no TUI dependency"
```

---

### Task 4: Chat API — wraps run_agent for HTTP context

**Files:**
- Create: `aivas/server/chat_api.py`
- Modify: `tests/server/test_endpoints.py` (add chat endpoint test)

**Interfaces:**
- Consumes: `aivas.tui.agent.run_agent(app, text, api_key, context)` where `app.conn = conn`
- Produces: `handle_chat(conn, text) -> tuple[str, tuple[str,int] | None]`
  - Returns `(response_text, (target, level))` if AI decided to scan, else `(response_text, None)`
- `POST /api/chat` → `{"response": str, "scan_id": str | null}`

- [ ] **Step 1: Write failing test**

Add to `tests/server/test_endpoints.py`:

```python
from unittest.mock import patch, AsyncMock
import aivas.server.main as _m


def test_chat_no_api_key(client, test_conn, monkeypatch):
    """Without an API key configured, chat returns a helpful error message."""
    monkeypatch.setattr(_m, "_conn", test_conn)
    with patch("aivas.server.chat_api.handle_chat",
               new=AsyncMock(return_value=("No AI configured.", None))):
        r = client.post("/api/chat", json={"text": "hello"})
    assert r.status_code == 200
    data = r.json()
    assert "response" in data
    assert data["scan_id"] is None


def test_chat_with_scan_intent(client, test_conn, monkeypatch):
    """When AI returns scan intent, response includes a non-null scan_id."""
    monkeypatch.setattr(_m, "_conn", test_conn)
    with patch("aivas.server.chat_api.handle_chat",
               new=AsyncMock(return_value=("Scanning now.", ("192.168.1.1", 2)))):
        r = client.post("/api/chat", json={"text": "scan 192.168.1.1"})
    assert r.status_code == 200
    data = r.json()
    assert data["scan_id"] is not None        # UUID string
    assert data["response"] == "Scanning now."
```

- [ ] **Step 2: Run tests, verify they fail**

```bash
cd /home/cyberpunk/aivas && pytest tests/server/test_endpoints.py::test_chat_no_api_key tests/server/test_endpoints.py::test_chat_with_scan_intent -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'aivas.server.chat_api'`

- [ ] **Step 3: Implement chat_api.py**

Create `aivas/server/chat_api.py`:

```python
"""Wraps the Groq agent for HTTP context — no TUI dependency at runtime."""
from __future__ import annotations

import os
import sqlite3
import types

from aivas import config as _config
from aivas.history import list_scans


def _build_context(conn: sqlite3.Connection) -> str:
    scans = list_scans(conn, limit=3)
    if not scans:
        return "No scans performed yet."
    lines = ["Recent scans:"]
    for s in scans:
        lines.append(f"  · {s['target']} — {s['grade']} ({s['risk_score']}/100)")
    return "\n".join(lines)


async def handle_chat(
    conn: sqlite3.Connection, text: str
) -> tuple[str, tuple[str, int] | None]:
    """Send text to the Groq agent. Returns (response, scan_intent|None).

    scan_intent is (target: str, level: int) when the agent wants to run a scan.
    Falls back to a plain error string when no API key is configured.
    """
    cfg = _config.load()
    api_key = cfg.get("api_key") or os.environ.get("GROQ_API_KEY")
    if not api_key:
        return (
            "No AI key configured. Run: aivas config set api_key YOUR_GROQ_KEY",
            None,
        )
    from aivas.tui.agent import run_agent

    # run_agent uses app.conn — provide a minimal shim
    holder = types.SimpleNamespace(conn=conn)
    context = _build_context(conn)
    try:
        response, scan_intent = await run_agent(holder, text, api_key, context=context)
        return response or "", scan_intent
    except Exception as exc:
        s = str(exc)
        if "401" in s or "invalid_api_key" in s.lower():
            return "API key rejected by Groq. Update it with: aivas config set api_key KEY", None
        return f"AI error: {exc}", None
```

- [ ] **Step 4: Run tests, verify they pass**

```bash
cd /home/cyberpunk/aivas && pytest tests/server/test_endpoints.py -v
```

Expected: all 7 tests PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/cyberpunk/aivas && git add aivas/server/chat_api.py tests/server/test_endpoints.py
git commit -m "feat(web): chat_api wraps run_agent for HTTP, POST /api/chat endpoint"
```

---

### Task 5: Complete frontend — full chat UI

**Files:**
- Modify: `frontend/index.html` (replace placeholder with full implementation)

**Interfaces:**
- Consumes: `GET /api/history?limit=1` (greeting), `POST /api/chat` (send message), `ws://…/ws/scan/{key}` (scan progress), `GET /api/history?limit=20` (history drawer)
- Produces: working browser UI matching the design spec

- [ ] **Step 1: Write the complete frontend**

Replace `frontend/index.html` with the full implementation:

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AIVAS — Network Security</title>
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
:root {
  --bg: #0a0a0a; --surface: #111; --raised: #161616;
  --border: #1e1e1e; --border-sub: #141414;
  --text: #e0e0e0; --muted: #666;
  --accent: #4a9eff;
  --crit: #ef5350; --high: #ff7043; --med: #fdd835; --low: #66bb6a;
  --user-bg: #0d1929; --user-border: #1a2d45; --user-text: #90bde0;
}
html, body { height: 100%; }
body { background: var(--bg); color: var(--text); font-family: system-ui,-apple-system,sans-serif;
       display: flex; flex-direction: column; overflow: hidden; }

/* ── Header ── */
#header { background: #0d0d0d; border-bottom: 1px solid #1a1a1a;
          padding: 0 24px; height: 48px; display: flex; align-items: center;
          flex-shrink: 0; gap: 10px; }
#logo { color: var(--accent); font-weight: 700; font-size: 15px; letter-spacing: .03em; }
#logo-sub { color: var(--muted); font-size: 11px; margin-top: 1px; }
#header-actions { margin-left: auto; display: flex; gap: 6px; }
.icon-btn { width: 32px; height: 32px; background: var(--raised); border: 1px solid #222;
            border-radius: 7px; display: flex; align-items: center; justify-content: center;
            cursor: pointer; color: var(--muted); font-size: 14px; transition: all .15s; }
.icon-btn:hover { border-color: #333; color: #aaa; }

/* ── Chat area ── */
#chat { flex: 1; overflow-y: auto; padding: 28px 0; scrollbar-width: thin;
        scrollbar-color: #222 transparent; }
#chat-inner { max-width: 800px; margin: 0 auto; padding: 0 24px;
              display: flex; flex-direction: column; gap: 20px; }

/* ── Messages ── */
.msg { display: flex; flex-direction: column; gap: 5px; }
.msg.user { align-items: flex-end; }
.msg.ai  { align-items: flex-start; }
.msg-label { font-size: 10px; font-weight: 700; letter-spacing: .07em;
             text-transform: uppercase; padding: 0 2px; }
.msg.ai  .msg-label { color: var(--accent); }
.msg.user .msg-label { color: var(--muted); }
.bubble { font-size: 14px; line-height: 1.7; color: #c8c8c8; max-width: 88%; }
.msg.user .bubble { background: var(--user-bg); border: 1px solid var(--user-border);
                    border-radius: 14px 14px 4px 14px; padding: 10px 15px;
                    color: var(--user-text); max-width: 72%; }
.bubble code { font-family: 'Fira Code','SF Mono','Courier New',monospace;
               color: #888; font-size: 12px; }
.thinking { display: flex; align-items: center; gap: 8px; color: var(--muted);
            font-size: 13px; font-style: italic; }
.spinner { color: var(--accent); display: inline-block; width: 14px; }

/* ── Scan result card ── */
.scan-card { background: var(--surface); border: 1px solid var(--border);
             border-radius: 10px; overflow: hidden; width: 100%; margin-top: 8px; }
.card-header { padding: 14px 18px; border-bottom: 1px solid #1a1a1a;
               display: flex; align-items: center; justify-content: space-between; }
.card-ip { font-family: 'Fira Code','SF Mono',monospace; font-size: 13px;
           color: var(--text); font-weight: 600; }
.card-meta { font-size: 11px; color: var(--muted); margin-top: 3px; }
.grade { font-size: 28px; font-weight: 800; line-height: 1; }
.grade-a { color: var(--low); } .grade-b { color: #aed581; }
.grade-c { color: var(--med); } .grade-d { color: var(--high); }
.grade-f { color: var(--crit); }
.sev-pills { padding: 10px 18px; display: flex; gap: 8px; border-bottom: 1px solid #1a1a1a; flex-wrap: wrap; }
.pill { font-size: 10px; font-weight: 700; padding: 4px 9px; border-radius: 5px; letter-spacing: .04em; }
.pill-crit { background:#1a0505; border:1px solid #3a1010; color:var(--crit); }
.pill-high { background:#1a0e05; border:1px solid #3a2010; color:var(--high); }
.pill-med  { background:#1a1505; border:1px solid #3a3010; color:var(--med); }
.pill-low  { background:#0a1a0a; border:1px solid #1a3a1a; color:var(--low); }
.cve-list { padding: 4px 0; }
.cve-row { display:flex; align-items:center; gap:10px; padding:8px 18px;
           border-bottom:1px solid var(--border-sub); font-size:12px; }
.cve-row:last-child { border-bottom: none; }
.sev-dot { width:7px; height:7px; border-radius:50%; flex-shrink:0; }
.dot-critical { background:var(--crit); } .dot-high { background:var(--high); }
.dot-medium { background:var(--med); } .dot-low { background:var(--low); }
.cve-id { font-family:'Fira Code','SF Mono',monospace; color:#888; font-size:11px;
          flex-shrink:0; min-width:120px; }
.cve-desc { color:#aaa; flex:1; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.cvss-score { font-family:'Fira Code','SF Mono',monospace; font-size:11px;
              color:var(--muted); flex-shrink:0; }
.show-all { padding:9px 18px; border-top:1px solid var(--border-sub);
            font-size:11px; color:var(--accent); cursor:pointer; }
.show-all:hover { color: #7ab8ff; }
.card-actions { padding:12px 18px; display:flex; gap:8px; border-top:1px solid #1a1a1a; flex-wrap:wrap; }
.act-btn { font-size:11px; font-weight:600; padding:7px 13px; border-radius:7px;
           cursor:pointer; border:1px solid #2a2a2a; background:var(--raised);
           color:#888; letter-spacing:.02em; transition:all .15s; }
.act-btn:hover { border-color:#3a3a3a; color:#bbb; }
.act-btn.primary { background:#0d1929; border-color:#1a3050; color:var(--accent); }
.act-btn.primary:hover { background:#0f1e35; }

/* ── Input bar ── */
#input-bar { background:#0d0d0d; border-top:1px solid #1a1a1a;
             padding:14px 24px; flex-shrink:0; }
#input-row { max-width:800px; margin:0 auto; display:flex; gap:10px; align-items:center; }
#text-input { flex:1; background:var(--raised); border:1px solid #262626;
              border-radius:10px; padding:11px 16px; font-size:14px; color:var(--text);
              font-family:inherit; outline:none; transition:border-color .15s; }
#text-input:focus { border-color:#2a3a4a; }
#text-input::placeholder { color:#444; }
#send-btn { width:38px; height:38px; background:#4a9eff22; border:1px solid #4a9eff44;
            border-radius:9px; color:var(--accent); font-size:18px;
            display:flex; align-items:center; justify-content:center;
            cursor:pointer; flex-shrink:0; transition:background .15s; }
#send-btn:hover { background:#4a9eff33; }

/* ── History drawer ── */
#drawer-overlay { position:fixed; inset:0; background:rgba(0,0,0,.5); z-index:40;
                  display:none; }
#history-drawer { position:fixed; top:0; right:0; width:340px; height:100%;
                  background:#0d0d0d; border-left:1px solid var(--border);
                  z-index:50; transform:translateX(100%); transition:transform .2s ease;
                  display:flex; flex-direction:column; }
#history-drawer.open { transform:translateX(0); }
.drawer-header { padding:16px 20px; border-bottom:1px solid var(--border);
                 display:flex; align-items:center; justify-content:space-between; }
.drawer-title { font-size:13px; font-weight:700; color:var(--text); }
.close-btn { cursor:pointer; color:var(--muted); font-size:18px; line-height:1; }
.close-btn:hover { color:var(--text); }
#drawer-list { flex:1; overflow-y:auto; padding:8px 0; }
.scan-row { padding:12px 20px; border-bottom:1px solid var(--border-sub);
            display:flex; align-items:center; cursor:pointer; gap:12px; }
.scan-row:hover { background:var(--raised); }
.scan-row-ip { font-family:'Fira Code','SF Mono',monospace; font-size:12px; color:var(--text); flex:1; }
.scan-row-meta { font-size:10px; color:var(--muted); margin-top:2px; }
.scan-row-grade { font-size:18px; font-weight:800; flex-shrink:0; }

/* ── Settings modal ── */
#modal-overlay { position:fixed; inset:0; background:rgba(0,0,0,.65); z-index:60; display:none;
                 align-items:center; justify-content:center; }
#modal-overlay.open { display:flex; }
#settings-modal { background:#0d0d0d; border:1px solid var(--border); border-radius:12px;
                  width:400px; max-width:90vw; padding:28px; }
.modal-title { font-size:15px; font-weight:700; margin-bottom:20px; color:var(--text); }
.field { margin-bottom:18px; }
.field label { display:block; font-size:11px; font-weight:600; color:var(--muted);
               letter-spacing:.06em; text-transform:uppercase; margin-bottom:6px; }
.field input { width:100%; background:var(--raised); border:1px solid #262626;
               border-radius:8px; padding:9px 12px; font-size:13px; color:var(--text);
               font-family:inherit; outline:none; }
.field input:focus { border-color:#2a3a4a; }
.field-hint { font-size:11px; color:var(--muted); margin-top:5px; }
.modal-actions { display:flex; gap:8px; justify-content:flex-end; margin-top:8px; }
.modal-btn { padding:8px 16px; border-radius:7px; font-size:12px; font-weight:600;
             cursor:pointer; border:1px solid #2a2a2a; background:var(--raised); color:#888; }
.modal-btn.primary { background:#0d1929; border-color:#1a3050; color:var(--accent); }
</style>
</head>
<body>

<div id="header">
  <div>
    <div id="logo">✦ AIVAS</div>
    <div id="logo-sub">Network Security</div>
  </div>
  <div id="header-actions">
    <div class="icon-btn" title="Scan history" onclick="openHistory()">🕐</div>
    <div class="icon-btn" title="Settings" onclick="openSettings()">⚙</div>
  </div>
</div>

<div id="chat"><div id="chat-inner"></div></div>

<div id="input-bar">
  <div id="input-row">
    <input id="text-input" type="text" placeholder="Ask anything, or scan a target…"
           autocomplete="off" />
    <div id="send-btn" onclick="handleSend()">↑</div>
  </div>
</div>

<!-- History drawer -->
<div id="drawer-overlay" onclick="closeHistory()"></div>
<div id="history-drawer">
  <div class="drawer-header">
    <div class="drawer-title">Scan History</div>
    <div class="close-btn" onclick="closeHistory()">✕</div>
  </div>
  <div id="drawer-list"></div>
</div>

<!-- Settings modal -->
<div id="modal-overlay" onclick="handleOverlayClick(event)">
  <div id="settings-modal">
    <div class="modal-title">Settings</div>
    <div class="field">
      <label>Groq API Key</label>
      <input id="api-key-input" type="password" placeholder="gsk_…" />
      <div class="field-hint">Or set via: <code>aivas config set api_key YOUR_KEY</code></div>
    </div>
    <div class="modal-actions">
      <div class="modal-btn" onclick="closeSettings()">Cancel</div>
      <div class="modal-btn primary" onclick="saveSettings()">Save</div>
    </div>
  </div>
</div>

<script>
const chatInner = document.getElementById('chat-inner');
const textInput = document.getElementById('text-input');
const spinnerChars = '⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏';
let spinnerIdx = 0;
let spinnerTimer = null;

/* ── Helpers ── */
function escHtml(s) {
  return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;')
                  .replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}

function scrollBottom() {
  document.getElementById('chat').scrollTop = 9999999;
}

function gradeClass(g) {
  return 'grade-' + (g || 'f').toLowerCase();
}

function startSpinner(el) {
  if (spinnerTimer) return;
  spinnerTimer = setInterval(() => {
    spinnerIdx = (spinnerIdx + 1) % spinnerChars.length;
    document.querySelectorAll('.spinner').forEach(s => {
      s.textContent = spinnerChars[spinnerIdx];
    });
  }, 100);
}

function stopSpinnerIfNoThinking() {
  if (!document.querySelector('.thinking')) {
    clearInterval(spinnerTimer);
    spinnerTimer = null;
  }
}

/* ── Message factories ── */
function appendMsg(role, html) {
  const div = document.createElement('div');
  div.className = `msg ${role}`;
  const label = role === 'ai' ? '✦ AIVAS' : 'You';
  div.innerHTML = `<div class="msg-label">${label}</div><div class="bubble">${html}</div>`;
  chatInner.appendChild(div);
  scrollBottom();
  return div;
}

function appendThinking(text) {
  const div = document.createElement('div');
  div.className = 'msg ai';
  div.innerHTML = `<div class="msg-label">✦ AIVAS</div>
    <div class="bubble thinking">
      <span class="spinner">⠋</span>
      <span class="thinking-text">${escHtml(text)}</span>
    </div>`;
  chatInner.appendChild(div);
  scrollBottom();
  startSpinner();
  return div;
}

function updateThinking(el, text) {
  const t = el.querySelector('.thinking-text');
  if (t) t.textContent = text;
  scrollBottom();
}

function removeThinking(el) {
  el.remove();
  stopSpinnerIfNoThinking();
}

function sevDotClass(sev) {
  const m = { CRITICAL:'critical', HIGH:'high', MEDIUM:'medium', LOW:'low' };
  return 'dot-' + (m[sev] || 'low');
}

function pillHtml(count, label, cls) {
  if (!count) return '';
  return `<span class="pill ${cls}">${count} ${label}</span>`;
}

function appendScanCard(ev) {
  const { target, grade, score, service_count, findings } = ev;
  const crit = findings.filter(f => f.cvss_severity === 'CRITICAL').length;
  const high = findings.filter(f => f.cvss_severity === 'HIGH').length;
  const med  = findings.filter(f => f.cvss_severity === 'MEDIUM').length;
  const low  = findings.filter(f => f.cvss_severity === 'LOW').length;

  const top5 = findings.slice(0, 5);
  const cveRowsHtml = top5.map(f => `
    <div class="cve-row">
      <div class="sev-dot ${sevDotClass(f.cvss_severity)}"></div>
      <div class="cve-id">${escHtml(f.cve_id)}</div>
      <div class="cve-desc">${escHtml((f.description || '').slice(0, 70))}</div>
      <div class="cvss-score">${f.cvss_score ?? 'N/A'}</div>
    </div>`).join('');

  const allCveRowsHtml = findings.map(f => `
    <div class="cve-row">
      <div class="sev-dot ${sevDotClass(f.cvss_severity)}"></div>
      <div class="cve-id">${escHtml(f.cve_id)}</div>
      <div class="cve-desc">${escHtml((f.description || '').slice(0, 70))}</div>
      <div class="cvss-score">${f.cvss_score ?? 'N/A'}</div>
    </div>`).join('');

  const showAllHtml = findings.length > 5
    ? `<div class="show-all" data-expanded="false"
          data-all="${escHtml(allCveRowsHtml)}"
          onclick="toggleFindings(this)">↓ Show all ${findings.length} findings</div>`
    : '';

  const tgt = escHtml(target || '');
  const div = document.createElement('div');
  div.className = 'msg ai';
  div.innerHTML = `
    <div class="msg-label">✦ AIVAS</div>
    <div class="scan-card">
      <div class="card-header">
        <div>
          <div class="card-ip">${tgt}</div>
          <div class="card-meta">${service_count} service(s) · ${findings.length} finding(s)</div>
        </div>
        <div class="grade ${gradeClass(grade)}">${escHtml(grade)}</div>
      </div>
      <div class="sev-pills">
        ${pillHtml(crit,'CRITICAL','pill-crit')}
        ${pillHtml(high,'HIGH','pill-high')}
        ${pillHtml(med,'MEDIUM','pill-med')}
        ${pillHtml(low,'LOW','pill-low')}
        ${(!crit && !high && !med && !low) ? '<span style="color:var(--low);font-size:11px;">No vulnerabilities found</span>' : ''}
      </div>
      <div class="cve-list" id="cve-list-${ev.scan_id}">${cveRowsHtml}</div>
      ${showAllHtml}
      <div class="card-actions">
        <button class="act-btn primary" onclick="sendText('narrate findings')">Narrate findings</button>
        <button class="act-btn" onclick="sendText('explain the worst CVE')">Explain worst CVE</button>
        <button class="act-btn" onclick="sendText('rescan ${tgt}')">Rescan</button>
      </div>
    </div>`;
  chatInner.appendChild(div);
  scrollBottom();
}

function toggleFindings(el) {
  const expanded = el.dataset.expanded === 'true';
  const listEl = el.previousElementSibling;
  if (!expanded) {
    listEl.innerHTML = el.dataset.all;
    el.textContent = '↑ Show fewer';
    el.dataset.expanded = 'true';
  } else {
    const all = el.dataset.all;
    const parser = new DOMParser();
    const doc = parser.parseFromString('<div>' + all + '</div>', 'text/html');
    const rows = doc.querySelectorAll('.cve-row');
    listEl.innerHTML = Array.from(rows).slice(0, 5).map(r => r.outerHTML).join('');
    el.textContent = `↓ Show all findings`;
    el.dataset.expanded = 'false';
  }
  scrollBottom();
}

/* ── WebSocket scan ── */
function openScanSocket(scanKey, thinkingEl) {
  const ws = new WebSocket(`ws://${location.host}/ws/scan/${scanKey}`);
  ws.onmessage = (e) => {
    const ev = JSON.parse(e.data);
    if (ev.type === 'progress') {
      updateThinking(thinkingEl, ev.text);
    } else if (ev.type === 'done') {
      removeThinking(thinkingEl);
      appendScanCard(ev);
      appendMsg('ai', 'Ask me to explain any CVE, or say <em style="color:var(--accent)">narrate in Swahili</em> for a bilingual report.');
    } else if (ev.type === 'error') {
      removeThinking(thinkingEl);
      appendMsg('ai', `⚠ ${escHtml(ev.text)}`);
    }
  };
  ws.onerror = () => {
    removeThinking(thinkingEl);
    appendMsg('ai', '⚠ WebSocket connection failed.');
  };
}

/* ── Send message ── */
async function sendText(text) {
  textInput.value = '';
  appendMsg('user', escHtml(text));
  const thinking = appendThinking('AIVAS is thinking…');

  let data;
  try {
    const res = await fetch('/api/chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text }),
    });
    data = await res.json();
  } catch (err) {
    removeThinking(thinking);
    appendMsg('ai', '⚠ Could not reach the server.');
    return;
  }

  if (data.scan_id) {
    updateThinking(thinking, 'Running port discovery…');
    if (data.response) appendMsg('ai', escHtml(data.response));
    openScanSocket(data.scan_id, thinking);
  } else {
    removeThinking(thinking);
    if (data.response) appendMsg('ai', escHtml(data.response));
  }
}

function handleSend() {
  const text = textInput.value.trim();
  if (!text) return;
  sendText(text);
}

textInput.addEventListener('keydown', e => {
  if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); handleSend(); }
});

/* ── History drawer ── */
function openHistory() {
  fetch('/api/history?limit=20')
    .then(r => r.json())
    .then(scans => {
      const list = document.getElementById('drawer-list');
      if (!scans.length) {
        list.innerHTML = '<div style="padding:20px;color:var(--muted);font-size:13px;">No scans yet.</div>';
      } else {
        list.innerHTML = scans.map(s => {
          const d = new Date(s.started_at);
          const ago = Math.floor((Date.now() - d) / 86400000);
          const agoStr = ago === 0 ? 'today' : ago === 1 ? 'yesterday' : `${ago} days ago`;
          return `<div class="scan-row" onclick="loadScan(${s.id}, '${escHtml(s.target)}'); closeHistory();">
            <div>
              <div class="scan-row-ip">${escHtml(s.target)}</div>
              <div class="scan-row-meta">${s.finding_count} findings · ${agoStr}</div>
            </div>
            <div class="scan-row-grade ${gradeClass(s.grade.replace('Grade ',''))}">${escHtml(s.grade.replace('Grade ',''))}</div>
          </div>`;
        }).join('');
      }
    });
  document.getElementById('history-drawer').classList.add('open');
  document.getElementById('drawer-overlay').style.display = 'block';
}

function closeHistory() {
  document.getElementById('history-drawer').classList.remove('open');
  document.getElementById('drawer-overlay').style.display = 'none';
}

function loadScan(id, target) {
  sendText(`summarize scan ${id} for ${target}`);
}

/* ── Settings modal ── */
function openSettings() {
  document.getElementById('modal-overlay').classList.add('open');
}

function closeSettings() {
  document.getElementById('modal-overlay').classList.remove('open');
}

function handleOverlayClick(e) {
  if (e.target === document.getElementById('modal-overlay')) closeSettings();
}

function saveSettings() {
  const key = document.getElementById('api-key-input').value.trim();
  if (key) {
    appendMsg('ai', `API key received. To persist it, run: <code>aivas config set api_key ${escHtml(key.slice(0,8))}…</code>`);
  }
  closeSettings();
}

/* ── Greeting on load ── */
async function init() {
  try {
    const history = await fetch('/api/history?limit=1').then(r => r.json());
    if (history.length > 0) {
      const last = history[0];
      const d = new Date(last.started_at);
      const daysAgo = Math.floor((Date.now() - d) / 86400000);
      const agoStr = daysAgo === 0 ? 'earlier today' : daysAgo === 1 ? 'yesterday' : `${daysAgo} days ago`;
      const grade = last.grade ? last.grade.replace('Grade ', '') : '?';
      appendMsg('ai',
        `Welcome back. Your last scan of <code>${escHtml(last.target)}</code> was ${agoStr} — ` +
        `Grade <span class="${gradeClass(grade)}" style="font-weight:700">${grade}</span>, ` +
        `${last.finding_count} findings. Want me to rescan, or would you like a summary?`
      );
    } else {
      appendMsg('ai',
        `Hello. I'm AIVAS, your network security assistant. ` +
        `Give me an IP address or network range and I'll scan it for you. ` +
        `You can also ask me about CVEs or say <em style="color:var(--accent)">/help</em> for more.`
      );
    }
  } catch {
    appendMsg('ai', 'Hello. I\'m AIVAS. Type an IP to scan or ask me anything about your network security.');
  }
  textInput.focus();
}

init();
</script>
</body>
</html>
```

- [ ] **Step 2: Start the server and test manually in a browser**

```bash
cd /home/cyberpunk/aivas && python -m aivas serve --open
```

Expected: browser opens at `http://127.0.0.1:8000`, shows the AIVAS header, chat area with greeting message, and input bar at the bottom.

**Verify each of these manually:**

1. Greeting appears automatically on load (either first-visit or returning-user message)
2. Type `hello` and press Enter — user bubble appears, thinking spinner shows, AI responds
3. Type `scan 192.168.1.1` — thinking shows "Running port discovery…", updates to steps, then shows a result card (or error if host is offline)
4. Click a card's "Narrate findings" button — sends that message to chat
5. Click 🕐 (history) — drawer slides in showing past scans
6. Click ⚙ (settings) — modal opens and closes
7. Type in Swahili: `angalia 192.168.1.1` — AI responds in Swahili

- [ ] **Step 3: Run full test suite**

```bash
cd /home/cyberpunk/aivas && pytest tests/ -v --tb=short 2>&1 | tail -25
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
cd /home/cyberpunk/aivas && git add frontend/index.html
git commit -m "feat(web): complete chat frontend — AI greeting, scan cards, history drawer, settings"
```

---

## Self-Review

**Spec coverage check:**

| Spec requirement | Task |
|---|---|
| Single-page, no sidebar | Task 5 — single column layout |
| Header: logo + two icon buttons | Task 5 — `#header` with 🕐 and ⚙ |
| AI-led greeting on load | Task 5 — `init()` calls `/api/history` |
| Natural language input | Task 4 + 5 — `POST /api/chat` |
| Scan result card inline | Task 5 — `appendScanCard()` |
| Real-time progress via WebSocket | Task 3 + 5 — `openScanSocket()` |
| Severity color language | Task 5 — CSS variables `--crit/high/med/low` |
| Grade badge color-coded | Task 5 — `grade-a` through `grade-f` CSS classes |
| History drawer | Task 5 — drawer HTML + `openHistory()` |
| Settings modal | Task 5 — modal HTML + `openSettings()` |
| `aivas serve` CLI command | Task 1 |
| FastAPI + uvicorn | Task 1 — pyproject.toml deps |
| `GET /api/history` | Task 1 (impl) + Task 2 (tests) |
| `GET /api/scan/{id}` | Task 1 (impl) + Task 2 (tests) |
| `POST /api/chat` | Task 4 (impl + tests) |
| `ws://.../ws/scan/{key}` | Task 1 (impl) |
| scan_worker no TUI deps | Task 3 |
| No Python file > 200 lines | All files planned at ≤ 150 lines |
| Bilingual auto-detect | Handled by `run_agent` (existing) |

**Placeholder scan:** None found.

**Type consistency:** `handle_chat` returns `tuple[str, tuple[str,int] | None]` — used correctly in `main.py` as `scan_intent` with `_pending[key] = scan_intent`. `run_scan` yields `dict` with consistent keys across all tasks.
